# Project

## Опис

Цей проект реалізує базові CRUD операції для категорій використовуючи FastAPI, SQLAlchemy та Pydantic.

## Запуск проекту

1. Встановіть залежності:
    ```
    pip install -r requirements.txt
    ```

2. Запустіть сервер:
    ```
    uvicorn app.main:app --reload
    ```

3. Відкрийте браузер і перейдіть за адресою `http://127.0.0.1:8000/docs` для перегляду автоматично згенерованої документації API.
